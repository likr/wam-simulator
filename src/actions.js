import { createAction } from 'redux-starter-kit'

export const updateParams = createAction('update-params')
export const updateDInput = createAction('update-d-input')
export const resetDInput = createAction('reset-d-input')
